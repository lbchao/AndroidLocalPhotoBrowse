<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="pop_window_style">
        <item name="android:windowEnterAnimation">@anim/pop_window_on</item>
        <item name="android:windowExitAnimation">@anim/pop_window_off</item>
    </style>

</resources>
